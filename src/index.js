export const rules = {
  'prefer-separate-exports': require('./rules/prefer-separate-exports'),
}

export const configs = {
  recommended: {
    rules: {
      'tgandrews/prefer-separate-exports': 2,
    }
  }
}